# Archived project material

The files under `engineering_notes/` record the chronological development of
OmniFetch/ALPS, including superseded plans and negative experiments. They are
kept for provenance and should not be treated as the final user guide or the
authoritative result set.

Canonical results and analysis live in `../docs/alps/`. The supported
reproduction entry point is `../run_alps.sh`.
