diff --git a/benchmark_models/debug_running/run_dinov2-small_debug.py b/benchmark_models/debug_running/run_dinov2-small_debug.py
index 7ce5d6f..d050014 100644
--- a/benchmark_models/debug_running/run_dinov2-small_debug.py
+++ b/benchmark_models/debug_running/run_dinov2-small_debug.py
@@ -26,7 +26,9 @@ from hexkl_utils import (
 
 def run(args):
     patch_dsp_heap_256mb()
-    wrapped, pixels = create_dinov2_debug_model_and_input()
+    wrapped, pixels = create_dinov2_debug_model_and_input(
+        num_layers=getattr(args, 'num_layers', 1)
+    )
     print_dinov2_debug_identity(wrapped, pixels)
     inputs = [pixels]
     module = compile_to_linalg(wrapped, tuple(inputs), decomp_pow=False)
@@ -67,7 +69,9 @@ def run(args):
 
     patched = None
     if args.enable_hexkl:
-        candidate, n_batch, n_f16 = apply_hexkl_ir_rewrites(ir)
+        candidate, n_batch, n_f16 = apply_hexkl_ir_rewrites(
+            ir, enable_m_pad=args.enable_omnifetch_m_pad_hmx
+        )
         patched = candidate if n_batch or n_f16 else None
         print(f"[HexKL] rewrites={n_batch + n_f16}")
     options = hexagon_options_phase4(
@@ -84,6 +88,7 @@ def run(args):
         disable_lwp_loop=args.disable_lwp_loop,
         omnifetch_items_through=args.omnifetch_items_through,
         enable_omnifetch_kv_vtcm=args.enable_omnifetch_kv_vtcm,
+        enable_omnifetch_m_pad_hmx=args.enable_omnifetch_m_pad_hmx,
         prefetch_baseline=args.prefetch_baseline,
         prefetch_baseline_distance=args.prefetch_baseline_distance,
         apt_get_hx_manual_candidate_ids=(
@@ -118,4 +123,10 @@ if __name__ == "__main__":
         default=1,
         help="Number of serial in-process executions averaged on the DSP.",
     )
+    parser.add_argument(
+        "--num-layers",
+        type=int,
+        default=1,
+        help="Number of transformer layers (1=debug, 4=medium, 12=full)",
+    )
     run(parser.parse_args())
diff --git a/benchmark_models/dinov2_debug_common.py b/benchmark_models/dinov2_debug_common.py
index eb78ab1..e7e8bdf 100644
--- a/benchmark_models/dinov2_debug_common.py
+++ b/benchmark_models/dinov2_debug_common.py
@@ -20,6 +20,7 @@ class Dinov2DebugWrapper(torch.nn.Module):
 def create_dinov2_debug_model_and_input(
     *,
     static_position_export: bool = False,
+    num_layers: int = 1,  # Add parameter
 ) -> tuple[torch.nn.Module, torch.Tensor]:
     """Create the exact model and logical input used by every backend."""
     torch.manual_seed(142)
@@ -28,7 +29,7 @@ def create_dinov2_debug_model_and_input(
         patch_size=8,
         num_channels=3,
         hidden_size=64,
-        num_hidden_layers=1,
+        num_hidden_layers=num_layers,  # Use parameter
         num_attention_heads=2,
         intermediate_size=128,
         hidden_act="gelu_new",
diff --git a/qcom_hexagon_backend/backend/hexagon_executor.py b/qcom_hexagon_backend/backend/hexagon_executor.py
index 53c1324..eb6bf97 100644
--- a/qcom_hexagon_backend/backend/hexagon_executor.py
+++ b/qcom_hexagon_backend/backend/hexagon_executor.py
@@ -508,7 +508,8 @@ class HexagonExecutor:
             ),
             # Run the kernel using run_main_on_hexagon
             (
-                "adb {} -s {} shell 'cd {}; export ADSP_LIBRARY_PATH={}; touch run_main_on_hexagon.farf; ./run_main_on_hexagon 3 {} stack_size=8388608'".format(
+                # Increased stack size from 8MB to 16MB to handle larger models
+                "adb {} -s {} shell 'cd {}; export ADSP_LIBRARY_PATH={}; touch run_main_on_hexagon.farf; ./run_main_on_hexagon 3 {} stack_size=16777216'".format(
                     self.config.env_vars["ANDROID_HOST"],
                     self.config.env_vars["ANDROID_SERIAL"],
                     self.device_path,
diff --git a/qcom_hexagon_backend/lib/Transforms/MatmulToHexKLPass.cpp b/qcom_hexagon_backend/lib/Transforms/MatmulToHexKLPass.cpp
index fb86d77..2d0001d 100644
--- a/qcom_hexagon_backend/lib/Transforms/MatmulToHexKLPass.cpp
+++ b/qcom_hexagon_backend/lib/Transforms/MatmulToHexKLPass.cpp
@@ -37,6 +37,22 @@ struct MatmulToHexKL final : public OpRewritePattern<linalg::MatmulOp> {
 
   bool enableAttentionHmx;
   bool enableMPadHmx;
+  
+  // EXPERIMENTAL: Track M-pad matmuls per function to avoid frame bug
+  mutable int mPadCountThisFunction = 0;
+  
+  // Dynamic limit based on buffer size to avoid frame-lowering stack-coloring defect.
+  // The Hexagon frame bug is triggered by cumulative pressure from multiple
+  // over-aligned dynamic stack allocations, not just count. Use a heuristic:
+  // Estimate total frame pressure = sum of (padded_M * max(K, N) * sizeof(fp16))
+  // Each M-pad matmul allocates 3 buffers: lhs[M×K], rhs[K×N], result[M×N].
+  mutable int64_t mPadFramePressure = 0;
+  
+  // Conservative limit derived from empirical testing:
+  // - 1 layer (6 M-pad, M=32, K=64-128, N=64-128): works (~120KB)
+  // - 2 layers (12 M-pad, ~240KB): testing boundary
+  // Try 240KB to allow full 2 layers
+  constexpr static int64_t kMaxMPadFramePressureBytes = 200 * 1024;  // 200 KB
 
   LogicalResult matchAndRewrite(linalg::MatmulOp op,
                                 PatternRewriter &rewriter) const override {
@@ -70,6 +86,23 @@ struct MatmulToHexKL final : public OpRewritePattern<linalg::MatmulOp> {
       return rewriter.notifyMatchFailure(
           op, "M not divisible by HMX tile size 32; enableMPadHmx off");
     }
+    
+    // Estimate frame pressure for this M-pad matmul
+    if (mUnaligned && enableMPadHmx) {
+      int64_t paddedM = ((M + kHmxTile - 1) / kHmxTile) * kHmxTile;
+      // Each M-pad allocates: lhs[paddedM×K], rhs[K×N], result[paddedM×N]
+      // All FP16 (2 bytes). The frame pressure is from the padded dimensions.
+      int64_t thisPressure = (paddedM * K + K * N + paddedM * N) * 2;
+      
+      if (mPadFramePressure + thisPressure > kMaxMPadFramePressureBytes) {
+        DBG("skip HexKL: M-pad frame pressure "
+            << mPadFramePressure << " + " << thisPressure << " > "
+            << kMaxMPadFramePressureBytes << " bytes; keep HVX");
+        return rewriter.notifyMatchFailure(
+            op,
+            "M-pad frame pressure limit reached; keep HVX to avoid frame bug");
+      }
+    }
     // M-pad allocates a fresh contiguous A/result buffer sized to the padded M.
     // At large N the resulting total function-frame pressure trips a Hexagon
     // frame-lowering stack-coloring defect (over-aligned dynamic frame clobbers
@@ -106,6 +139,12 @@ struct MatmulToHexKL final : public OpRewritePattern<linalg::MatmulOp> {
       // internally with fresh contiguous buffers (mirroring its N-pad path),
       // which avoids the tensor.pad/extract_slice buffers that faulted on
       // device at large N.
+      if (mUnaligned) {
+        mPadCountThisFunction++;  // Track M-pad conversions
+        // Update cumulative frame pressure
+        int64_t paddedM = ((M + kHmxTile - 1) / kHmxTile) * kHmxTile;
+        mPadFramePressure += (paddedM * K + K * N + paddedM * N) * 2;
+      }
       rewriter.replaceOpWithNewOp<hexkl::MatmulOp>(op, C.getType(), A, B, C);
       return success();
     }
